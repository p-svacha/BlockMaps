Materials in this directory should never be used directly.
They serve as a data storage for color, texture and texture scaling that BlendableSurfaceShader uses for drawing multiple blended textures.

All textures used by materials here must be 1024x1024.